import { ArrowRight, CalendarDays, ExternalLink, Images, Info, Layers3, MapPin, ShieldAlert } from "lucide-react";
import Image from "next/image";
import Link from "next/link";
import type { DemoCase } from "@/lib/benchmarks";

const classifications = { benchmark_ground_truth: "Benchmark annotation", curated_expected_behavior: "Curated expected behavior", illustrative_only: "Illustrative only" };

export function DemoGallery({ cases }: { cases: DemoCase[] }) {
  return <section className="demo-gallery-grid" aria-label="Verified SatQuery demonstration cases">{cases.map((item, index) => <article className="demo-case-card" key={item.demo_id}><div className="demo-case-media"><Image src={item.primary_image} alt={`${item.title} primary satellite image`} fill sizes="(max-width: 768px) 100vw, 45vw" priority={index === 0}/>{item.secondary_image && <Image src={item.secondary_image} alt={`${item.title} secondary satellite image`} fill sizes="(max-width: 768px) 50vw, 22vw" className="demo-secondary" priority={index === 0}/>}<span>{item.workflow.replaceAll("_", " ")}</span></div><div className="demo-case-body"><div className="demo-case-tags"><span><ShieldAlert size={12}/>{classifications[item.expected_result_classification]}</span><span>{item.difficulty}</span></div><h2>{item.title}</h2><p className="demo-purpose">{item.purpose}</p><dl><div><dt><MapPin size={13}/>Source</dt><dd>{item.source}</dd></div><div><dt><Images size={13}/>Modality</dt><dd>{item.modality}</dd></div>{item.dates.length > 0 && <div><dt><CalendarDays size={13}/>Dates</dt><dd>{item.dates.join(" → ")}</dd></div>}<div><dt><Layers3 size={13}/>Required</dt><dd>{item.required_specialists.join(" · ")}</dd></div></dl><blockquote><strong>Suggested question</strong>{item.question}</blockquote><p className="demo-expected"><Info size={15}/><span>{item.expected_behavior}</span></p><details><summary>Attribution and limitations</summary><p>{item.attribution}</p><p>{item.license}</p><ul>{item.limitations.map(limit => <li key={limit}>{limit}</li>)}</ul></details><Link href={item.launch_path} className="demo-launch">Open guided workspace <ArrowRight size={15}/></Link><small><ExternalLink size={12}/>Guided launch uses the real application pipeline; imagery is not auto-injected.</small></div></article>)}</section>;
}
